#include<stdio.h>
main()
{
	int a,b,c,d;
	printf("enter the marks");
	scanf("%d%d%d%d",&a,&b,&c,&d);
	if (a>=35 && b>=35 && c>=35 && d>=35)
     {
     	printf("PASS");
	 }
	 else 
	 {
	 	printf("FAIL");
	 }
	
}
